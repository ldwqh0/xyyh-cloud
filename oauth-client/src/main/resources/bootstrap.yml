spring:
  application:
    name: oauth-client
  cloud:
    config:
      uri: http://localhost:8000/
      profile: test
security:
  oauth2:
    client:
      client-authentication-scheme: form