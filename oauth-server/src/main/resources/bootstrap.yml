server:
  servlet:
    session:
      cookie:
        name: OAUTH2SESSION
spring:
  application:
    name: oauth-server
  cloud:
    config:
      uri: http://localhost:8000/
      profile: test
security:
  oauth2:
    client:
      authentication-scheme: form